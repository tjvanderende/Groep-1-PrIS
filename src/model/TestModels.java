package model;

import java.util.ArrayList;
import java.util.Arrays;

public class TestModels {
	private static PrIS system = new PrIS();
	ArrayList<Docent> docenten = new ArrayList<Docent>();
	public static void main(String[] arg){
		TestModels.printList();
		//TestModels.printList(TestModels.printList(system.getLessenByDocent(new Docent("jos", "tes123", "bar.vaneijkelenburg@hu.nl") -> ));
	}

	private static void printList(){
	/*	for(Les les : TestModels.system.getLessenByPerson(null)) {
			System.out.println(les);
		}*/
	}
}
